import { createStyles, getStylesRef, rem } from "@mantine/core";
export const useDemoStyles = createStyles(() => ({
  heading: {
    border: "1px solid red",
    backgroundColor: "black",
    color: "white",
  },
}));
